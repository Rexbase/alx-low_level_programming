#include <stdio.h>

// A simple C function that adds two numbers
int add(int a, int b) {
    return a + b;
}

// Another C function that subtracts two numbers
int subtract(int a, int b) {
    return a - b;
}
